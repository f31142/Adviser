services:
  adviser:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file: .env
    environment:
      DATA_DIR: /app/data
      PORT: 3000
    volumes:
      - adviser-data:/app/data
volumes:
  adviser-data:
