# GitHub 중심으로 전환

2026-09-17, 로컬에 남아 있던 기존 Adviser 소스를 회수해 `f31142/Adviser`의 독립 실행 프로젝트로 전환했습니다. 이전의 준비 문서만 있던 상태를 실행 코드·DB 마이그레이션·테스트·배포 구성으로 바꿉니다.

- React 서비스 화면과 기존 업무 API를 기반으로 사용했습니다.
- Sites/Vinext/Cloudflare 호스팅 연결과 ChatGPT 인증 의존성을 제거했습니다.
- Node.js 24 HTTP 서버, SQLite 및 비공개 파일 디렉터리로 실행합니다.
- 신청 → 관리자 승인 → 결제 흐름을 유지하고 반려와 온라인 결제 어댑터, 계정 복구/초기화 CLI를 추가했습니다.
- 테스트·환경변수 예시·잠금 파일·Docker·Render 구성·GitHub Actions를 함께 관리합니다.
- 실제 고객 계정·채팅·서류·DB·기존 비밀값은 가져오지 않았습니다.

사용자가 기존 Sites 프로젝트를 삭제했으므로 새 배포는 새 데이터베이스로 시작합니다. 기존 Sites 프로젝트를 복구하거나 재게시하지 않습니다. 과거 프로젝트 식별자는 `source-provenance.json`의 이력 정보일 뿐 배포 설정으로 사용하지 않습니다.

외부 호스팅 계정 연결, 운영 관리자 비밀값, 계좌 정보, 가맹점 결제 키와 실제 결제 검증이 남아 있습니다. 공개 주소의 배포 완료와 소스 저장 완료는 별개입니다.
